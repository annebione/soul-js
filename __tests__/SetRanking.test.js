import * as jasmineCheck from 'jasmine-check';

import '../resources/jest.d.ts';

import { SetRanking } from '../src/lib/SetRanking';

jasmineCheck.install();


describe('SetRanking', () => {
    it('Should return best and bottom ranked items in array of objects by number', () => {

    })
})
